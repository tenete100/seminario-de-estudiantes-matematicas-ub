---
title: "Seminario de Estudiantes de Matemáticas"
subtitle: "Universitat de Barcelona"
---

## Bienvenidos

El **Seminario de Estudiantes de Matemáticas** es una iniciativa organizada por estudiantes de la Facultad de Matemáticas de la Universitat de Barcelona. Nuestro objetivo es crear un espacio donde los estudiantes puedan presentar y discutir temas matemáticos de interés, fomentando el aprendizaje colaborativo y la divulgación.

## Próximas Sesiones

```{python}
#| echo: false
#| output: asis
import yaml
from pathlib import Path
from datetime import datetime

# Buscar todos los seminarios
seminarios_dir = Path('seminarios')
seminarios = []

for carpeta in seminarios_dir.iterdir():
    if carpeta.is_dir():
        yaml_file = carpeta / 'seminario.yml'
        if yaml_file.exists():
            with open(yaml_file, 'r', encoding='utf-8') as f:
                data = yaml.safe_load(f)
                data['_carpeta'] = carpeta.name
                data['_tipo'] = 'completo'
                seminarios.append(data)
        else:
            # Seminario antiguo solo con PDF
            pdfs = list(carpeta.glob('*.pdf'))
            if pdfs:
                # Extraer info del nombre de carpeta (YYYY-semestre-tema)
                partes = carpeta.name.split('-')
                if len(partes) >= 3:
                    año = partes[0]
                    semestre = partes[1]
                    tema = ' '.join(partes[2:]).title()
                    seminarios.append({
                        'info': {
                            'titulo': tema,
                            'subtitulo': f'{semestre.title()} {año}',
                            'estado': 'completado'
                        },
                        '_carpeta': carpeta.name,
                        '_tipo': 'pdf',
                        '_pdf': pdfs[0].name
                    })

# Buscar próximas sesiones (planificadas)
proximas = []
for sem in seminarios:
    if sem.get('_tipo') == 'completo':
        for sesion in sem.get('sesiones', []):
            if sesion.get('estado') == 'planificada':
                proximas.append({
                    'seminario': sem['info'].get('titulo', ''),
                    'sesion': sesion,
                    'carpeta': sem['_carpeta']
                })

# Ordenar por fecha
try:
    proximas.sort(key=lambda x: x['sesion'].get('fecha', '9999'))
except:
    pass

if proximas:
    print("::: {.callout-tip}")
    print("## 📅 Próximas sesiones")
    print("")
    for p in proximas[:3]:  # Mostrar máximo 3
        fecha = p['sesion'].get('fecha', 'TBD')
        hora = p['sesion'].get('hora', '')
        lugar = p['sesion'].get('lugar', '')
        ponente = p['sesion'].get('ponente', '')
        print(f"**{p['seminario']}** - Sesión {p['sesion'].get('numero', '')}: {p['sesion'].get('titulo', '')}")
        print(f"")
        print(f"- 📆 {fecha} {hora}")
        if ponente:
            print(f"- 🎤 Ponente: {ponente}")
        if lugar:
            print(f"- 📍 {lugar}")
        print("")
    print(":::")
else:
    print("::: {.callout-note}")
    print("## Próximas sesiones")
    print("No hay sesiones programadas. Consulta la página de [Seminarios](seminarios.qmd) para ver el archivo completo.")
    print(":::")
```

## Seminarios por Curso Académico

```{python}
#| echo: false
#| output: asis
import yaml
from pathlib import Path

# Buscar todos los seminarios
seminarios_dir = Path('seminarios')
seminarios = []

for carpeta in seminarios_dir.iterdir():
    if carpeta.is_dir():
        yaml_file = carpeta / 'seminario.yml'
        if yaml_file.exists():
            with open(yaml_file, 'r', encoding='utf-8') as f:
                data = yaml.safe_load(f)
                data['_carpeta'] = carpeta.name
                data['_tipo'] = 'completo'
                seminarios.append(data)
        else:
            # Seminario antiguo solo con PDF
            pdfs = list(carpeta.glob('*.pdf'))
            if pdfs:
                partes = carpeta.name.split('-')
                if len(partes) >= 3:
                    año = partes[0]
                    semestre = partes[1]
                    tema = ' '.join(partes[2:]).replace('-', ' ').title()
                    seminarios.append({
                        'info': {
                            'titulo': tema,
                            'subtitulo': f'{semestre.title()} {año}',
                            'estado': 'completado'
                        },
                        '_carpeta': carpeta.name,
                        '_tipo': 'pdf',
                        '_pdf': pdfs[0].name
                    })

# Extraer año de la carpeta y agrupar por curso académico
def get_curso(carpeta):
    partes = carpeta.split('-')
    if len(partes) >= 2:
        año = int(partes[0])
        semestre = partes[1].lower()
        # Otoño pertenece al curso que empieza ese año
        # Primavera pertenece al curso que empezó el año anterior
        if semestre == 'primavera':
            return f"{año-1}-{año}"
        else:
            return f"{año}-{año+1}"
    return "Otros"

# Agrupar por curso
cursos = {}
for sem in seminarios:
    curso = get_curso(sem['_carpeta'])
    if curso not in cursos:
        cursos[curso] = []
    cursos[curso].append(sem)

# Ordenar cursos (más reciente primero)
cursos_ordenados = sorted(cursos.keys(), reverse=True)

# Estado badges
estado_icon = {
    'planificado': '🗓️',
    'en-curso': '🔄',
    'completado': '✅'
}

for curso in cursos_ordenados:
    print(f"### Curso {curso}")
    print("")
    print("| Seminario | Estado |")
    print("|-----------|--------|")
    
    for sem in cursos[curso]:
        info = sem.get('info', {})
        titulo = info.get('titulo', 'Sin título')
        estado = info.get('estado', 'planificado')
        icon = estado_icon.get(estado, '📅')
        
        if sem['_tipo'] == 'completo':
            titulo_link = f"[{titulo}](seminarios/{sem['_carpeta']}/)"
        else:
            titulo_link = f"[{titulo}](seminarios/{sem['_carpeta']}/{sem['_pdf']})"
        
        print(f"| 📘 **{titulo_link}** | {icon} {estado} |")
    
    print("")
```

## ¿Quieres participar?

Si estás interesado en dar una charla o proponer un tema, contacta con nosotros a través del correo del seminario o acércate a cualquiera de los organizadores.

## Enlaces útiles

- [Facultat de Matemàtiques i Informàtica - UB](https://mat.ub.edu/)
- [Repositorio GitHub del seminario](https://github.com/carlosferlo/seminario-de-estudiantes-matematicas-ub)
